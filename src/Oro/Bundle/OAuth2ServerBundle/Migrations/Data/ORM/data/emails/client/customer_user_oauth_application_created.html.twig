{# @name = customer_user_oauth_application_created #}
{# @entityName = Oro\Bundle\OAuth2ServerBundle\Entity\Client #}
{# @subject = OAuth application added to your account #}
{# @isSystem = 1 #}
{# @isEditable = 1 #}

{% extends oro_get_email_template('base_storefront') %}

{% block content %}
    <h1 style="{{ styleH1 }}">Hello, {{ entity.customerUser }}.</h1>

    <!--empty space-->
    <table role="presentation" border="0" cellspacing="0" cellpadding="0">
        <tr>
            <td height="12" style="{{ styleEmptyRowH12 }}">&nbsp;</td>
        </tr>
    </table>

    <p style="{{ styleP }}">A new OAuth application "{{ entity.name }}" was recently added to your account.</p>
    <p style="{{ styleP }}">This application is authorized to use Web API.</p>
    <p style="{{ styleP }}">Please contact the administrator if you are not aware of this change to your account.</p>
{% endblock %}
